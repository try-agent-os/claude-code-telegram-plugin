{
  "name": "telegram",
  "version": "0.1.0",
  "description": "Telegram bot interface for Claude Code. Chat with your agent from Telegram — text, voice, photos, documents, links.",
  "author": {
    "name": "try-agent-os",
    "url": "https://github.com/try-agent-os"
  },
  "homepage": "https://github.com/try-agent-os/claude-code-telegram-plugin",
  "repository": "https://github.com/try-agent-os/claude-code-telegram-plugin",
  "license": "MIT",
  "keywords": ["telegram", "chat", "bot", "mcp", "communication"],
  "userConfig": {
    "bot_token": {
      "title": "Bot token",
      "type": "string",
      "sensitive": true,
      "required": true,
      "description": "Telegram bot API token from @BotFather"
    },
    "admin_user_ids": {
      "title": "Admin user IDs",
      "type": "string",
      "required": true,
      "description": "Comma-separated Telegram user IDs of authorized admins (get yours via @userinfobot)"
    },
    "admin_usernames": {
      "title": "Admin usernames",
      "type": "string",
      "description": "Comma-separated Telegram usernames for display (parallel to admin_user_ids)"
    }
  }
}
